# T-006 - Flujo de intencion a plan

Version: 1.2.0
Estado: Cerrada y congelada (completada, verificada, integrada y publicada)
Prioridad: Alta

## Objetivo

Implementar la version inicial del Planner (ARQ-004): transformar la intencion del usuario en un plan de ejecucion estructurado y trazable, a partir del `ProjectContext` reconstruido por T-005, sin depender del historial de conversaciones, y entregar un plan consumible por T-007 (Builder) y por la CLI.

El plan no se improvisa (ADN Condor): se genera de forma determinista desde el contexto operativo y las recomendaciones entregadas por T-005.

## Alcance

T-006 debe transformar la intencion del usuario en un plan dentro de los limites aprobados:

- Consumir el `ProjectContext` persistido (via `IStateStore.LoadContextAsync`) y su `ProjectContextSummary`, `ContinuationPoint`, `ContextRisk`, `RelevantDependency` y `PlannerRecommendation`.
- Consumir una solicitud libre del usuario en texto (espanol), sin intencion libre procesada por LLM en esta version.
- Interpretar la intencion de forma acotada y determinista: diferenciar entre idea nueva, continuar el proyecto activo y modificar el proyecto activo, mediante heuristica textual de patrones acotados.
- Generar un `WorkPlan` determinista y trazable: objetivo derivado, descomposicion en `PlanTask` ordenadas con dependencias, prioridad y evidencia.
- Capturar en el plan las recomendaciones para Planner y los riesgos considerados entregados por T-005.
- Exponer la capacidad mediante un comando publico nuevo: `condor planear` (texto y `--json`), en espanol sin tildes.
- Persistir `plan.json` como artefacto derivado y transitorio en el estado local.

## No alcance

T-006 NO debe:

- implementar cambios en el proyecto objetivo (frontera T-007);
- re-ejecutar el descubrimiento de proyecto (frontera T-004);
- re-ejecutar la reconstruccion de contexto ni la lectura de artefactos operativos (frontera T-005);
- interpretar la intencion libre con LLM ni Ollama en esta version inicial;
- usar red, servicios cloud ni descargas;
- escribir conocimiento permanente en el repositorio del proyecto objetivo;
- modificar `assessment.json`, `context.json` ni sus esquemas; solo genera y persiste `plan.json` como salida derivada;
- analizar codigo, dependencias transitivas ni deuda tecnica;
- inventar tareas, objetivos ni evidencias sin base en el contexto o en la solicitud.

## Frontera con T-005

T-005 construyo el contexto operativo y produjo recomendaciones para Planner. T-006 consume ese contexto para interpretar la intencion del usuario y generar el plan. Esa capacidad de "interpretar la intencion" fue delegada a T-006 en T-005 (DEC-028).

```
T-005: ProjectProfile → contexto → comprension → punto de continuacion → recomendaciones
T-006: ProjectContext + solicitud del usuario → interpretacion de intencion → WorkPlan
```

## Frontera con T-007

T-006 produce el plan de trabajo (tareas ordenadas con dependencias). T-007 (Builder inicial) consume ese plan para implementar cambios en el proyecto objetivo. T-006 no implementa cambios ni genera codigo.

## Frontera con T-004

T-004 entrego `ProjectProfile`. T-005 lo consumio. T-006 consume `ProjectContext` (que ya incorpora el `ProjectProfile`) y no re-descubre el proyecto.

## Entradas

- `ProjectContext` persistido (via `IStateStore.LoadContextAsync`): `ProjectContextSummary`, `ContinuationPoint`, `ContextRisk`, `RelevantDependency`, `PlannerRecommendation`, `LimitsApplied`.
- Solicitud libre del usuario en texto (espanol), minima y sin formato impuesto.
- Limites centralizados del plan (`PlanLimits`).

## Salidas

- `WorkPlan` (plan de ejecucion) con:
  - estado del plan y motivo de degradacion;
  - raiz del proyecto activo;
  - intencion interpretada (nueva / continuar / modificar / indefinida);
  - objetivo derivado;
  - tareas ordenadas con dependencias, prioridad y evidencia;
  - evidencias consideradas (recomendaciones y riesgos de T-005);
  - limites aplicados y degradaciones declaradas;
  - metadata de generacion.
- Seccion `PLAN` en el resultado en texto de `condor planear`.
- Campo `plan` en la salida de `condor planear --json`.

## Responsabilidades

| Capa | Responsabilidad |
|---|---|
| Condor.Core | Modelos de plan (`WorkPlan`, `PlanTask`, `PlanLimits`), logica pura de generacion e interpretacion (`PlanGenerator`): sin IO, sin frameworks, determinista. |
| Condor.Infrastructure | `PlanService` (orquestacion de carga de contexto, lectura de solicitud y delegacion al generador) y persistencia del plan derivado (`plan.json`). |
| Condor.Cli | Comando `condor planear` (texto y `--json`), ayuda y estado inicial. |

Ningun componente de T-006 modifica el proyecto objetivo ni persiste conocimiento permanente.

## Contrato del Planner

### Interfaz de servicio

En `Condor.Core.Contracts`, siguiendo el patron de `IContextService`:

```
IPlanService
  Task<WorkPlan> BuildPlanAsync(string userRequest, CancellationToken cancellationToken = default)
```

`PlanService` (Infrastructure) implementa `IPlanService`: carga el `ProjectContext` persistido, recibe la solicitud del usuario y delega la generacion pura en `PlanGenerator` (Core).

### Modelo tecnico consolidado

El modelo tecnico se consolida por la decision D-E1: el concepto de dominio "plan de trabajo" se materializa tecnicamente como `WorkPlan` en `Condor.Core.Models`, con `SchemaVersion` propio.

```
ProjectContext                    // consumido; salida de T-005
  + Summary, ContinuationPoint, Risks, RelevantDependencies, Recommendations, LimitsApplied

WorkPlan                          // nueva salida del Planner
  SchemaVersion         string                     // "1.0.0", independiente del Context
  Status                DetectionStatus            // Detected, NotDetected, Error o Limited
  Reason                string?
  RootName              string
  Intention             string                     // "nueva" | "continuar" | "modificar" | "indefinida"
  Objective             string
  Tasks                 List<PlanTask>             // ordenadas por dependencia y prioridad
  Evidence              List<string>               // evidencias consideradas (recomendaciones y riesgos)
  RisksConsidered       List<string>
  LimitsApplied         List<string>
  GeneratedAtUtc        DateTime

PlanTask
  Id                    string                     // "T0", "T1", ...
  Title                 string
  Detail                string?
  DependsOn             List<string>
  Priority              string                     // "alta" | "media" | "baja"
  Evidence              string
```

Reglas de compatibilidad:

- El modelo del Context no se modifica; el plan es una salida derivada.
- El modelo es versionable con `SchemaVersion` propio.
- Los nombres del JSON forman parte del esquema tecnico y pueden permanecer en ingles; el texto visible del usuario es espanol sin tildes.

### CLI

Capacidad expuesta mediante un comando publico nuevo, coherente con el contrato en espanol (DEC-025):

```
condor planear "<solicitud>"
condor planear "<solicitud>" --json
```

- `condor planear "<solicitud>"`: genera el plan y muestra la seccion `PLAN`.
- `condor planear "<solicitud>" --json`: emite el resultado en formato JSON.
- Sin `ProjectContext` persistido: salida degradada con motivo y mensaje instructivo para ejecutar primero `condor contexto` y `condor analizar`; no es una excepcion.
- Sin solicitud valida: salida degradada con motivo; no se inventa un objetivo.

## Dependencias

Consume:

- T-005: `ProjectContext` (persistido en `context.json`) y `PlannerRecommendation`.
- T-001/T-004: `AssessmentResult` / `ProjectProfile` embebidos en el contexto.
- Patrones de servicio y persistencia de T-001/T-004/T-005 (`IStateStore`, `LocalStateStore`, serializacion UTF-8 sin BOM).

Sin acoplamiento funcional con T-002 y T-003: T-006 no usa LLM ni el recomendador en esta version.

Produce para:

- T-007 (Builder inicial): consume `WorkPlan` como entrada.
- CLI: capacidad directa para el usuario.

## Persistencia

- `plan.json` en `%LOCALAPPDATA%\Condor\state\` (directorio de estado local, DEC-008).
- Es un artefacto derivado y transitorio: regenerable en cualquier momento, no es conocimiento permanente del proyecto.
- No se escribe en el repositorio del proyecto objetivo.
- No se modifica `assessment.json` ni `context.json`.

## Degradaciones

| Situacion | Comportamiento |
|---|---|
| Sin `ProjectContext` persistido | Estado `NotDetected` con motivo instructivo (ejecutar `condor contexto` / `condor analizar`); sin excepcion |
| Solicitud del usuario vacia o ilegible | Estado `Limited` con motivo; sin inventar objetivo |
| Contexto en `Limited` | Plan parcial valido; se declara en `LimitsApplied`; sin error |
| Contexto sin proyecto | Plan parcial valido sin raiz; sin error |
| La falla de una parte | Nunca destruye el plan completo |

## Criterios de aceptacion

- Plan real y verificable sobre repositorios reales: sobre el repositorio Condor, sin contexto, contexto degradado y con solicitudes nuevas/continuar/modificar.
- Determinista: misma solicitud y mismo contexto producen el mismo plan, con la unica excepcion de `GeneratedAtUtc`.
- Funciona sin internet, sin LLM y sin servicios cloud.
- No modifica el proyecto objetivo ni el context.
- Degradaciones correctas, sin excepciones.
- Compatibilidad con contextos previos (sin project).
- Frontera T-005/T-006/T-007 respetada: no reconstruye contexto, no implementa cambios.
- CLI `condor planear` (texto y `--json`) funcional y en espanol sin tildes.
- Evidencia automatizada (unitarias, integracion, arquitectura) y E2E real.
- Documentacion operativa e inventarios actualizados.
- Continuidad posible sin esta conversacion.

## Pruebas necesarias

- Unitarias (Core): generacion de plan desde intencion nueva/continuar/modificar, descomposicion en tareas, orden y dependencias, captura de recomendaciones y riesgos, limites, degradaciones, compatibilidad sin proyecto y determinismo.
- Integracion (Infrastructure): `PlanService`, persistencia de `plan.json`, contexto real, sin contexto, contexto sin proyecto, contexto `Limited`, solicitud vacia.
- Arquitectura: logica pura en Condor.Core sin IO; referencias de capas sin ruptura.
- Compatibilidad: contexto previo sin project produce plan parcial valido.
- Determinismo: dos ejecuciones sobre el mismo contexto y solicitud producen la misma serializacion (excepto `GeneratedAtUtc`).
- Contrato CLI: `condor planear`, `condor planear --json` y degradacion sin contexto.
- E2E real: ejecucion sobre el propio repositorio Condor.

## Archivos que deberan crearse o modificarse (implementacion)

Crear:

- `Src/Condor.Core/Models/WorkPlan.cs`, `PlanTask.cs` y `PlanLimits.cs`.
- `Src/Condor.Core/Planning/PlanGenerator.cs` (logica pura) y `PlanIntent.cs`.
- `Src/Condor.Core/Contracts/IPlanService.cs`.
- `Src/Condor.Core/Serialization/PlanJson.cs`.
- `Src/Condor.Infrastructure/Planning/PlanService.cs`.
- `Src/Condor.Cli/Commands/PlanCommand.cs` y `Src/Condor.Cli/Presentation/PlanRenderer.cs`.
- Pruebas de unidad, integracion, arquitectura y E2E.
- Estado derivado `plan.json` (generado por ejecucion, no versionado).

Modificar:

- `Src/Condor.Core/Contracts/IStateStore.cs` (extension aditiva: `SavePlanAsync` y `LoadPlanAsync`).
- `Src/Condor.Infrastructure/State/LocalStateStore.cs` (persistencia de `plan.json`, UTF-8 sin BOM).
- `Src/Condor.Cli/Program.cs`, ayuda y estado inicial (comando `condor planear`).
- `Docs/02_Memoria/INVENTARIO_ARQUITECTURA.md` (ARQ-004 a En desarrollo / Implementado segun avance).
- `Docs/02_Memoria/INVENTARIO_FUNCIONAL.md` (capacidad de plan).
- Artefactos operativos: `ESTADO_DESARROLLO.md`, `BACKLOG.md`, `KANBAN.md`, `RELEVO.md` y `REGISTRO_CAMBIOS.md`.

## Diseno tecnico

El diseno tecnico de T-006 se consolida a continuacion, siguiendo los patrones de T-004 y T-005 (D-D5, D-D6, D-D11, D-D12).

### Arquitectura

Separa la logica pura (Core) de la orquestacion y persistencia (Infrastructure) y de la presentacion (Cli), respetando la frontera de capas.

```
Condor.Core
  Models       WorkPlan, PlanTask, PlanLimits
  Planning     PlanGenerator (logica pura), PlanIntent (clasificacion de intencion)
  Contracts    IPlanService
  Serialization PlanJson

Condor.Infrastructure
  Planning     PlanService
  State        LocalStateStore (extension aditiva SavePlanAsync/LoadPlanAsync)

Condor.Cli
  Commands     PlanCommand
  Presentation PlanRenderer
```

### Modelos

En `Condor.Core.Models`, coherentes con D-E1 y con las claves JSON en ingles (camelCase, patron de `ContextJson`/`AssessmentJson`):

```
WorkPlan
  SchemaVersion         string                   // "1.0.0"
  Status                DetectionStatus          // Detected | Limited | NotDetected | Error
  Reason                string?
  RootName              string
  WorkingDirectory      string
  Intention             string                   // "nueva" | "continuar" | "modificar" | "indefinida"
  Objective             string
  Tasks                 List<PlanTask>
  Evidence              List<string>             // evidencias consideradas (recomendaciones y riesgos de T-005)
  RisksConsidered       List<string>
  LimitsApplied         List<string>
  GeneratedAtUtc        DateTime

PlanTask
  Id                    string                   // "T0", "T1", ...
  Title                 string
  Detail                string?
  DependsOn             List<string>
  Priority              string                   // "alta" | "media" | "baja"
  Evidence              string

PlanLimits
  MaxTasks                    int    // propuesto: 12
  MaxObjectiveLength          int    // propuesto: 240
  MaxTaskDetailLength         int    // propuesto: 320
  MaxEvidenceItems            int    // propuesto: 30
  PlanTimeoutMilliseconds     int    // propuesto: 15_000
  static Default             PlanLimits
```

`DetectionStatus` se reutiliza (patron D-D2); no se amplia.

### Contratos e interfaces

En `Condor.Core.Contracts`:

```
IPlanService
  Task<WorkPlan> BuildPlanAsync(string userRequest, CancellationToken cancellationToken = default)
```

`IStateStore` se extiende de forma aditiva (D-E4):

```
IStateStore
  Task SavePlanAsync(WorkPlan plan, CancellationToken ct = default)
  Task<WorkPlan?> LoadPlanAsync(CancellationToken ct = default)
```

### Flujo de datos

```
condor planear "<solicitud>"
  → PlanCommand.ExecuteAsync(planService, stateStore, args)
  → PlanService.BuildPlanAsync(request)
      → stateStore.LoadContextAsync()            // ProjectContext persistido
      → si no hay contexto: devolver WorkPlan NotDetected (motivo instructivo)
      → PlanGenerator.Generate(context, request, PlanLimits.Default)
      → WorkPlan
  → stateStore.SavePlanAsync(plan)                // persiste plan.json (derivado)
  → texto: PlanRenderer.RenderPlan(plan)
    json: Console.WriteLine(PlanJson.Serialize(plan))
  → exit code: 1 si Status == NotDetected, si no 0
```

### Codificacion de la intencion (D-E3, detalle PROPUESTA)

`PlanIntent.Classify(string request)` devuelve uno de: `nueva`, `continuar`, `modificar` o `indefinida`, por heuristica textual ordinal sobre la solicitud normalizada en espanol sin tildes:

- `nueva`: terminos "nueva", "nuevo", "crear", "crea", "inicia un nuevo", "genera un proyecto", "desde cero", "prototipo";
- `continuar`: terminos "continuar", "continua", "sigue", "retoma", "siguiente tarea", "prosigue", "avanza";
- `modificar`: terminos "modificar", "modifica", "agregar", "agrega", "anade", "anadir", "implementar", "implementa", "cambia", "mejora", "corrige", "extiende", "refactoriza";
- si ninguno aplica: `indefinida`.

La clasificacion es determinista: la primera categoria que coincide (en orden nueva → continuar → modificar) define la intencion. Sin intencion reconocida se utiliza `indefinida` y el plan queda en `Limited` con motivo.

### Generacion del plan (D-E5, Patron de logica pura)

`PlanGenerator.Generate(ProjectContext? context, string request, PlanLimits limits)` (Core, sin IO):

1. Si `context` es null: devolver `WorkPlan` con `Status = NotDetected` y motivo instructivo de T-005 (`IStateStore` no distingue causa; se usa el motivo unico del contrato).
2. Si `request` es vacia o en blanco: devolver `WorkPlan` con `Status = Limited` y motivo "no hay solicitud del usuario".
3. Determinar `Intention` mediante `PlanIntent.Classify(request)`.
4. Si la intencion es `indefinida`: `Status = Limited` con motivo, conservando contexto parcial.
5. Derivar `Objective` a partir de la solicitud (normalizada y truncada a `MaxObjectiveLength`) y del contexto (rootName).
6. Construir `Tasks`:
   - tareas base segun la intencion (nueva: bootstrapping; continuar: retomar punto de continuacion; modificar: cambio puntual);
   - una tarea por cada `PlannerRecommendation` de T-005 (hasta `MaxTasks`);
   - tareas derivadas de riesgos `ContextRisk` (con prioridad segun severidad: alta/media/baja);
   - se truncan a `MaxTasks` conservando el orden ordinal.
7. Registrar en `Evidence` y `RisksConsidered` las recomendaciones y riesgos considerados (hasta `MaxEvidenceItems`), ordenados ordinalmente y sin duplicados.
8. `DependsOn` de cada `PlanTask` apunta a los `Id` de tareas previas de las que depende (por orden de generacion y prioridad).

Determinismo (D-E7): todas las colecciones se ordenan ordinalmente; la unica excepcion es `GeneratedAtUtc`.

### Responsabilidades de capa

- Core: `PlanGenerator`, `PlanIntent`, modelos y `PlanJson` (sin IO). El generador recibe el contexto ya cargado y la solicitud; no abre archivos.
- Infrastructure: `PlanService` carga el contexto persistido y delega en `PlanGenerator`; `LocalStateStore` persiste `plan.json` con UTF-8 sin BOM (patron D-D4).
- Cli: `PlanCommand` orquesta servicio + persistencia + salida; `PlanRenderer` muestra la seccion `PLAN`.

### Persistencia

- `plan.json` en `%LOCALAPPDATA%\Condor\state\`; artefacto derivado transitorio (patron D-D9).
- Serializacion UTF-8 sin BOM (patron D-D4 / correccion BOM de T-004).
- No modifica `assessment.json` ni `context.json`; no escribe en el repositorio del proyecto objetivo.

### Degradaciones

| Situacion | Comportamiento |
|---|---|
| Sin `ProjectContext` persistido | `NotDetected` con motivo instructivo (ejecutar `condor contexto` / `condor analizar`); exit code 1 |
| Solicitud vacia o en blanco | `Limited` con motivo; sin inventar objetivo |
| Intencion `indefinida` | `Limited` con motivo; contexto parcial valido |
| Contexto en `Limited` o sin proyecto | Plan parcial valido; se declara en `LimitsApplied`; sin error |
| La falla de una parte | Nunca destruye el plan completo |

### Limites

Definidos en `PlanLimits` (D-DE3, aprobados): `MaxTasks = 12`, `MaxObjectiveLength = 240`, `MaxTaskDetailLength = 320`, `MaxEvidenceItems = 30`, `PlanTimeoutMilliseconds = 15_000`. El modelo `PlanLimits` queda centralizado y versionable (patron D-D12).

### Determinismo

Misma solicitud y mismo contexto producen el mismo `WorkPlan`; la unica excepcion es `GeneratedAtUtc`. El generador ordena todas las colecciones ordinalmente y la lectura del contexto usa un orden fijo.

### CLI

```
condor planear "<solicitud>"
condor planear "<solicitud>" --json
```

- Texto: seccion `PLAN` con estado, intencion, objetivo y tareas (con prioridad y dependencias).
- `--json`: emite `WorkPlan` serializado en JSON.
- Sin contexto: `NotDetected`, mensaje instructivo y exit code 1.
- Espanol sin tildes (DEC-025).

### Fronteras

- T-005/T-006: T-006 consume `ProjectContext`; no reconstruye contexto ni lee artefactos.
- T-006/T-007: T-006 produce `WorkPlan`; no implementa cambios.
- T-004/T-006: T-006 no re-descubre el proyecto.

### Decisiones del diseno

- D-E1 a D-E8: aprobadas por el contrato (DEC-030). No se modifican.
- Decisiones tecnicas del diseno (valores de `PlanLimits`, patrones de intencion, reglas de orden y dependencias, serializacion `PlanJson`): aprobadas (DEC-031, D-DE1 a D-DE6).

## Decisiones

- La formalizacion del contrato de T-006 se registra como decision formal (DEC-030).
- El diseno tecnico completo de T-006 se registra como DEC-031 (D-DE1 a D-DE6), aprobada para implementacion.
- Cualquier decision adicional no prevista se detendra y se reportara antes de registrarse.

## Cierre formal

T-006 queda **cerrada y congelada**.

- Estado: cerrada y congelada (esta version, v1.2.0).
- Alcance aprobado, contrato (DEC-030) y diseno tecnico (DEC-031, D-DE1 a D-DE6 aprobadas) no se modifican.
- Evidencia de cierre:
  - Build Release: 0 errores, 0 advertencias.
  - Unitarias (Condor.Core): 113/113 correctas (intencion nueva/continuar/modificar/indefinida, generacion de tareas, orden, dependencias, prioridades, recomendaciones, riesgos, limites, degradaciones y determinismo).
  - Integracion (Condor.Infrastructure): 102/102 correctas (PlanService, carga de context.json, persistencia de plan.json, contexto existente/sin Project/Limited/ausente y solicitud vacia; CLI planear/--json/sin contexto).
  - Arquitectura: 13/13 correctas (Core sin IO, PlanGenerator/PlanIntent puros, referencias de capas).
  - CLI: `condor planear` (texto y `--json`) en espanol sin tildes; exit code 1 sin contexto.
  - E2E real sobre el repositorio Condor: intenciones nueva, continuar y modificar; uso del punto de continuacion de T-005.
  - Determinismo (D-E7): dos ejecuciones reales sobre el mismo estado producen el mismo plan salvo GeneratedAtUtc.
- Commits publicados en origin/main; HEAD == origin/main y working tree limpio al cierre.
- Cualquier mejora posterior del Planner debera registrarse como nueva tarea, decision o deuda; no se introduce dentro de T-006.
- Siguiente tarea: T-007 (Builder inicial), pendiente y no iniciada.

## Estado de formalizacion

- Reconocimiento: completado y aprobado.
- Formalizacion contractual: completada (contrato v1.0.0; DEC-030).
- Diseno tecnico: completado y aprobado (v1.1.0; DEC-031, D-DE1 a D-DE6).
- Implementacion: completada y verificada integralmente.
- Estado: T-006 — COMPLETADA, VERIFICADA, INTEGRADA, PUBLICADA Y CONGELADA.

## Regla de inicio

Antes de implementar: revision formal del diseno aprobado. No hacer commit, push ni merge sin autorizacion explicita.
